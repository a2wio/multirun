{
  "name": "multirun-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only window onto the bench: results db + artifacts volume, nothing else.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "marked": "^15.0.7",
    "pg": "^8.16.0",
    "ws": "^8.18.2"
  }
}
