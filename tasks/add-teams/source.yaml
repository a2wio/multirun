# kubeden/app-nextjs-01 — the Neon web-app template, private. The
# runner's init container clones it with the read-only deploy key
# (Secret: source-deploy-key). The sha is the template's import commit:
# every run starts from exactly this tree or the comparison means nothing.
repo: git@github.com:kubeden/app-nextjs-01.git
ref: 0d5826c43bf7876a8563ca373138cb83a85683d4
seed: seed
